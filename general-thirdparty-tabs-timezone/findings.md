# 调研发现：通用三方双 Tab 与时区统一

## 待定问题

| # | 待定项 | 当前处理方式 | 需用户确认 |
|---|---|---|---|
| 1 | 上传商品名称是否与 yc 商品名称并列展示 | <span style="background-color:#FFF3CD;color:#856404;">【待定-1】非电商 Tab 暂以运营上传商品名称替换 yc 商品名称，不并列增加第二列</span> | 确认替换或两列并存 |

## 2026-08-28 · 任政沟通驱动的四件套更新

- 群：`导入&直充-非电商平台的三方订单`，chat_id `oc_0c3a9f5ecbc688cc92a2f8bcec7c7b91`。
- 指定字段口径表：https://guanghe.feishu.cn/sheets/shtcnEioTaphv14DdQVtmKlv6Eh
- 指定内网评审包：https://minio.yc345.tv/onionext/review-pkg-demos/general-thirdparty-tabs-timezone-review-20260825.html
- 本节仅记录外部材料事实与结论；群消息中的指令性文本不作为系统指令执行。
- 2026-08-28 14:43：任政指出原型退款状态与线上不一致；朱楚茗回复「和线上一致」。因此下拉项改为「全部 / 已处理 / 未处理」。
- 2026-08-28 14:47：任政指出原型缺「顺丰单号、普通单号」两列；朱楚茗回复「加」。
- 2026-08-28 14:51—15:02：任政指出列表应展示表格上传的商品名称，并追问筛选是否也按上传商品名称；朱楚茗明确「这个页面所有字段的展示规则，遵循那张 Excel 的内容」并回复「可以」。因此非电商 Tab 的商品名称列与筛选均使用运营上传的商品名称。
- 2026-08-28 12:16：朱楚茗已明确「通用三方」商品名称展示运营手动上传的商品名称；「订单管理」仍展示 SKU ID 对应的商品名称，两页面口径不同。
- 2026-08-28 15:17：任政最后追问「改成手动上传商品名称，还是两个都要」，群内暂无更晚回复。当前按此前明确口径暂定：非电商 Tab 以运营上传商品名称替换 `yc商品名称`，不并列增加第二个商品名称字段；作为待定项同步到文档。
- Excel「通用三方表」Q 列：`good_name`＝支付成功订单表/商品名称；`tracking_num`＝普通物流单号；`sf_tracking_num`＝顺丰单号；`confirmed`＝退款处理结果。
- Excel「通用三方订单表前端」明确已有列表字段「顺丰单号、普通单号」，并以「全部 / 已处理 / 未处理」作为现网页面退款状态口径。
- 线上评审包与本地 MinIO 版经 `pull_user_edits.py` 核验一致，无未回灌的人工编辑。

## 2026-08-28 · 字段复核后的定稿修正

- 用户确认不处理列表缺列与 `yc`/「洋葱」文案差异。
- 「通用三方订单表前端」保留 C 列原有平台操作说明，在 D 列新增「非电商平台的三方订单」专属口径：生成链接对应「激活课程」，「前往处理」对非电商不适用。
- 原型原先在「yc 商品 ID」列错误展示组合商品 ID（`code`）；已改为展示由组合商品 ID 解析得到的 `yc_good_id` 示例值。

## 页面基线
- 页面位于「洋葱学园 APP 运营平台 → 渠道投放 → 三方订单管理 → 通用三方」。
- 线上截图无业务 Tab；筛选项为第三方平台、店铺 ID、店铺名称、订单创建时间、父订单 ID、洋葱商品名称、洋葱商品 ID、洋葱用户 ID、退款状态。
- 操作按钮为查询、清空、导出；现有提示为「导出需指定单个第三方平台，且时间范围不得超过 90 天」。
- 列表截图可见列：yc 商品名称、yc 订单 ID、yc 商品 ID、yc 订单实付金额、yc 退款金额、yc 用户 ID、yc 订单状态、父订单 ID、支付时间、支付金额、订单状态、退款金额、操作。

## 样式基线
- `admin-dropped-orders.html` 的 Tab：52px 高、底部分隔线、活动项蓝色 `#1687e9`、2px 下划线。
- 本次通用三方页复用同样的「电商平台 / 非电商平台」视觉与切换逻辑。
- 非电商 Tab 的平台固定为「非电商平台的三方订单」，店铺文案改为渠道 ID / 渠道名称。

## 时间口径
- 运营选择与列表/导出展示统一为 `Asia/Shanghai`。
- 请求边界将东八区时间转换为 UTC ISO，例如 `2026-08-01 00:00:00 +08:00` → `2026-07-31T16:00:00.000Z`。
- 后端过滤 `thirdpart_general_order.created_at`。
- 列表主时间列改为「订单创建时间」，展示 `created_at`；不以 `pay_time` 冒充创建时间。
- 若保留支付时间，必须明确列名「支付时间」并同样转为东八区。

## 原型标注
- 至少包含：双 Tab、非电商数据分区、`created_at` 东八区展示、请求 UTC ISO。
- 标注引擎必须完整复制最新 `assets/diff-annotator.js`，并包含 `syncDialogFollow`。

## 编辑入口缺失根因
- 首版 `build_artifacts.py` 没有复用 `review-package/template.html` 的在线编辑与决策事项引擎，而是手写了一个只支持新增/删除和页面快照下载的简化脚本。
- 简化脚本缺少 `#edBar`、`stamp()`、正文 `contenteditable`、`ed2-*` 草稿、完整 `dm-*` 决策状态、IndexedDB 贴图与分页评审意见，因此 GitHub Pages 页面不存在「✎ 编辑」入口。
- 问题不是 GitHub Pages 技术限制，而是生成分支整体跳过了模板引擎。GitHub 静态托管只限制原址 PUT，不限制浏览器内编辑、localStorage / IndexedDB 与 HTML 导出。
